# -----------------------------------------------------------------------------
# AI Chatbot Template
# -----------------------------------------------------------------------------
# Duplicate this file as `.env.local` (or `.env`) and fill in the variables you
# actually need. Never commit real API keys to source control.
# -----------------------------------------------------------------------------

# --- Core Vercel AI SDK defaults ------------------------------------------------

# Primary provider you intend to use. (openai, anthropic, google, groq, mistral, etc.)
AI_PROVIDER=deepseek

# Default model that your API route should invoke.
AI_MODEL=deepseek-chat

# Optional tuning knobs passed to the SDK call (numbers are strings in env files).
AI_TEMPERATURE=0.4
AI_MAX_TOKENS=2048

# Override the base URL if you proxy through a custom gateway.
# Leave blank when calling provider hosted APIs directly.
AI_BASE_URL=https://api.deepseek.com/v1

# If you run through a hosted gateway that requires a project-scoped API key.
AI_API_KEY=sk-78459cbe87624cda96974dc6b73b6dd6
AI_PROJECT_ID=deepseek-chat

# --- Provider specific keys -----------------------------------------------------
# Populate only the providers you plan to call. The Vercel AI SDK automatically
# picks the correct key based on the provider you instantiate in your API route.

QWEN_API_KEY=sk-bb95b8c492114d099bf16d0a0b1fcb38
DOUBAO_API_KEY=306d06b0-bdde-4207-8e12-447fa3d28f59
DEEPSEEK_API_KEY=sk-78459cbe87624cda96974dc6b73b6dd6

# --- Supabase ------------------------------------------------------------------
# Server-side: keep these secret
SUPABASE_URL=https://ycdbmdeljcioldyknqne.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InljZGJtZGVsamNpb2xkeWtucW5lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM0MjI4NzYsImV4cCI6MjA3ODk5ODg3Nn0.kXTrFt9GUsTkGokPLH6wCkTWjyGMYdS-Y4u6Ob4n_QY
# Optional service role key (DO NOT expose to client)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InljZGJtZGVsamNpb2xkeWtucW5lIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MzQyMjg3NiwiZXhwIjoyMDc4OTk4ODc2fQ.p-NXLvu5A9uNqFAIrasO2TW45MeumYzk1j9PPxU9QvU

# Client-side: only expose safe, public values (Astro exposes PUBLIC_* to the client)
PUBLIC_SUPABASE_URL=https://ycdbmdeljcioldyknqne.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InljZGJtZGVsamNpb2xkeWtucW5lIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjM0MjI4NzYsImV4cCI6MjA3ODk5ODg3Nn0.kXTrFt9GUsTkGokPLH6wCkTWjyGMYdS-Y4u6Ob4n_QY

# --- Convex --------------------------------------------------------------------
# Use either public URL (recommended for clients) or deployment slug for tooling.
NEXT_PUBLIC_CONVEX_URL=https://quaint-bloodhound-285.convex.cloud
PUBLIC_CONVEX_URL=https://quaint-bloodhound-285.convex.cloud
CONVEX_URL=https://quaint-bloodhound-285.convex.cloud
CONVEX_DEPLOYMENT=quaint-bloodhound-285

# --- Optional observability / telemetry ----------------------------------------
# Uncomment when forwarding usage metrics to Vercel Observability.
# VERBOSE_AI_LOGGING=true
# VERCEL_LOG_LEVEL=debug
# AI_DEBUG=1

# -----------------------------------------------------------------------------
# Tips:
# 1. Keep `.env.local` out of git (already ignored by the template).
# 2. When deploying to Vercel, define the same variables in the dashboard under
#    Project Settings → Environment Variables.
# 3. In development, restart `pnpm dev` whenever you edit env vars.
# -----------------------------------------------------------------------------
